<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.amayadream.bookstore.dao.IBookDao">
    <resultMap id="BaseResultMap" type="com.amayadream.bookstore.pojo.Book">
        <id column="BOOKID" property="bookid" jdbcType="VARCHAR"/>
        <result column="NAME" property="name" jdbcType="VARCHAR"/>
        <result column="PICTURE" property="picture" jdbcType="VARCHAR"/>
        <result column="DESCRIPTION" property="description" jdbcType="VARCHAR"/>
        <result column="TYPE" property="type" jdbcType="VARCHAR"/>
        <result column="STATUS" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="base">
        BOOKID, NAME, PICTURE, DESCRIPTION, TYPE, STATUS
    </sql>

    <!-- 查询所有用户 -->
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM
        (
        SELECT A.*,ROWNUM RN
        FROM
        (
        SELECT * FROM BS_BOOK
        ORDER BY FIRSTTIME
        ) A
        )
        WHERE RN BETWEEN #{start} and #{end}
    </select>

    <select id="selectByType" resultMap="BaseResultMap">
        SELECT * FROM
        (
        SELECT A.*,ROWNUM RN
        FROM
        (
        SELECT * FROM BS_BOOK
        WHERE TYPE = #{type, jdbcType=VARCHAR}
        ORDER BY FIRSTTIME
        ) A
        )
        WHERE RN BETWEEN #{start} and #{end}
    </select>

    <!-- 查询用户总数 -->
    <select id="selectAllCount" resultMap="BaseResultMap" >
        SELECT COUNT(*) USERID
        FROM BS_BOOK
    </select>

    <select id="selectByTypeCount" resultMap="BaseResultMap" >
        SELECT COUNT(*) USERID
        FROM BS_BOOK
        WHERE TYPE = #{type, jdbcType=VARCHAR}
    </select>

    <!-- 根据用户id查询用户 -->
    <select id="selectByBookid" resultMap="BaseResultMap" parameterType="string">
        SELECT
        <include refid="base"/>
        FROM BS_BOOK
        WHERE BOOKID = #{bookid, jdbcType=VARCHAR}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.amayadream.bookstore.pojo.Book">
        INSERT INTO BS_BOOK (BOOKID, NAME, PICTURE, DESCRIPTION, TYPE, STATUS)
        VALUES (sys_guid(), #{name, jdbcType=VARCHAR}, #{picture, jdbcType=VARCHAR}, #{description, jdbcType=VARCHAR},
        #{type, jdbcType=VARCHAR},  #{status, jdbcType=INTEGER}
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.amayadream.bookstore.pojo.Book">
        UPDATE BS_BOOK
        <set>
            <if test="name != null and name != ''">
                NAME = #{name, jdbcType=VARCHAR},
            </if>
            <if test="picture != null and picture != ''">
                PICTURE = #{picture, jdbcType=VARCHAR},
            </if>
            <if test="description != null and description != ''">
                DESCRIPTION = #{description, jdbcType=VARCHAR},
            </if>
            <if test="type != null and type != ''">
                TYPE = #{type, jdbcType=VARCHAR},
            </if>
            <if test="status != null and status != ''">
                STATUS = #{status, jdbcType=INTEGER},
            </if>
        </set>
        WHERE BOOKID = #{booid, jdbcType=VARCHAR}
    </update>

    <!-- 删除用户 -->
    <delete id="delete" parameterType="string">
        DELETE FROM BS_BOOK
        WHERE BOOKID = #{booid, jdbcType=VARCHAR}
    </delete>

</mapper>