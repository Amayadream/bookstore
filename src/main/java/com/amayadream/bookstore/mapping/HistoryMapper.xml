<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.amayadream.bookstore.dao.IHistoryDao">
    <resultMap id="BaseResultMap" type="com.amayadream.bookstore.pojo.History">
        <id column="ID" property="id" jdbcType="VARCHAR"/>
        <result column="USERID" property="userid" jdbcType="VARCHAR"/>
        <result column="BOOKID" property="bookid" jdbcType="VARCHAR"/>
        <result column="FIRSTTIME" property="firsttime" jdbcType="VARCHAR"/>
        <result column="LASTTIME" property="lasttime" jdbcType="VARCHAR"/>
        <result column="UNTIL" property="until" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="base">
        ID, USERID, BOOKID, FIRSTTIME, LASTTIME, UNTIL
    </sql>

    <!-- 查询所有用户 -->
    <select id="selectByUserid" resultMap="BaseResultMap">
        SELECT * FROM
        (
        SELECT A.*,ROWNUM RN
        FROM
        (
        SELECT * FROM BS_HISTORY
        WHERE USERID = #{userid, jdbcType=VARCHAR}
        ORDER BY FIRSTTIME
        ) A
        )
        WHERE RN BETWEEN #{start} and #{end}
    </select>

    <!-- 查询用户总数 -->
    <select id="count" resultMap="BaseResultMap" >
        SELECT COUNT(*) USERID
        FROM BS_HISTORY
    </select>

    <!-- 根据用户id查询用户 -->
    <select id="selectById" resultMap="BaseResultMap" parameterType="string">
        SELECT
        <include refid="base"/>
        FROM BS_HISTORY
        WHERE ID = #{id, jdbcType=VARCHAR}
    </select>

    <select id="selectByBookid" resultMap="BaseResultMap" parameterType="string">
        SELECT
        <include refid="base"/>
        FROM BS_HISTORY
        WHERE BOOKID = #{bookid, jdbcType=VARCHAR}
        AND USERID = #{userid, jdbcType=VARCHAR}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.amayadream.bookstore.pojo.History">
        INSERT INTO BS_HISTORY (ID, USERID, BOOKID, FIRSTTIME, LASTTIME, UNTIL)
        VALUES (sys_guid(), #{userid, jdbcType=VARCHAR}, #{bookid, jdbcType=VARCHAR}, #{firsttime, jdbcType=VARCHAR},
        #{lasttime, jdbcType=VARCHAR},  #{until, jdbcType=VARCHAR}
    </insert>

    <!-- 更新用户 -->
    <update id="update" parameterType="com.amayadream.bookstore.pojo.History">
        UPDATE BS_HISTORY
        <set>
            <if test="userid != null and userid != ''">
                USERID = #{userid, jdbcType=VARCHAR},
            </if>
            <if test="bookid != null and bookid != ''">
                BOOKID = #{bookid, jdbcType=VARCHAR},
            </if>
            <if test="firsttime != null and firsttime != ''">
                FIRSTTIME = #{firsttime, jdbcType=VARCHAR},
            </if>
            <if test="lasttime != null and lasttime != ''">
                LASTTIME = #{lasttime, jdbcType=VARCHAR},
            </if>
            <if test="until != null and until != ''">
                UNTIL = #{until, jdbcType=VARCHAR},
            </if>
        </set>
        WHERE ID = #{id, jdbcType=VARCHAR}
    </update>

    <!-- 删除用户 -->
    <delete id="delete" parameterType="string">
        DELETE FROM BS_HISTORY
        WHERE ID = #{id, jdbcType=VARCHAR}
    </delete>

    <delete id="deleteByBook" parameterType="string">
        DELETE FROM BS_HISTORY
        WHERE BOOKID = #{booid, jdbcType=VARCHAR}
        AND USERID = #{userid, jdbcType=VARCHAR}
    </delete>

</mapper>